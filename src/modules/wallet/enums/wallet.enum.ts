export enum CurrencyCode {
    GOLD = "gold",
    GEM = "gem",
    DUST = "dust",
}

export enum TransactionType {
    EARN = "earn",
    SPEND = "spend",
    REFUND = "refund",
    ADMIN = "admin",
    REWARD = "reward",
}

export enum TransactionSource {
    BATTLE = "battle",
    PURCHASE = "purchase",
}
